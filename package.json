{
  "name": "contours-to-classification-server",
  "version": "0.0.1",
  "scripts": {
    "setup": "npm run --prefix src/client/ setup && npm run --prefix src/server/ setup",
    "build": "rm -rf ./build/ && npm run --prefix src/client/ build && npm run --prefix src/server/ build",
    "dev": "./node_modules/.bin/nodemon",
    "production": "npm run build && NODE_ENV=production node ./build/server/server.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "npm": "^8.3.1"
  },
  "type": "module",
  "module": "es2020"
}