{
  "name": "contours-to-classification-base",
  "version": "0.0.1",
  "scripts": {
    "child": "npm run --prefix $npm_config_dir",
    "client": "npm run child --dir=./client/",
    "server": "npm run child --dir=./server/",
    "tests": "npm run child --dir=./tests/",
    "setup": "npm run tests setup && npm run client setup && npm run server setup",
    "test": "npm run tests test",
    "dev": "npm run client dev & npm run server dev",
    "build": "npm run server build && npm run client build",
    "cleanup": "npm run --prefix ./client/ cleanup && npm run --prefix ./server/ cleanup && rm -rf ./build/ "
  },
  "devDependencies": {
    "npm": "^8.3.1"
  },
  "type": "module",
  "module": "es2020"
}